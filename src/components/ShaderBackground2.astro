---
/**
 * ShaderBackground2 — Full-viewport WebGL canvas (radial color pulse effect)
 * Renders animated radial gradient with pulsing color bands.
 * Film grain is applied via a CSS overlay at native resolution
 * to stay crisp regardless of WebGL canvas downscaling.
 */
---

<canvas id="shader-bg2" aria-hidden="true" transition:persist="shader-bg2"></canvas>
<div id="noise-overlay2" aria-hidden="true" transition:persist="noise-overlay2"></div>

<style>
  #shader-bg2 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
  }

  #noise-overlay2 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    pointer-events: none;
    opacity: 0.1;
    mix-blend-mode: overlay;
    background-repeat: repeat;
    background-size: 128px 128px;
  }
</style>

<script>
  import { createShaderProgram, createFullscreenQuad, resizeCanvasToDisplaySize } from '../lib/webgl-utils';
  import { noiseDataUri } from '../lib/noise-texture';
  import vertSrc from '../shaders/background.vert?raw';
  import fragSrc from '../shaders/background2.frag?raw';

  function initBackground2() {
    if ((window as any).__SHADER2_INIT_DONE__) return;
    (window as any).__SHADER2_INIT_DONE__ = true;

    const canvas = document.getElementById('shader-bg2') as HTMLCanvasElement;
    if (!canvas) return;

    // Apply noise overlay texture (base64-inlined, no HTTP request)
    const noiseEl = document.getElementById('noise-overlay2') as HTMLDivElement;
    if (noiseEl) {
      noiseEl.style.backgroundImage = `url("${noiseDataUri}")`;
    }

    const gl = canvas.getContext('webgl2', { alpha: false, antialias: false });
    if (!gl) {
      console.warn('[ShaderBackground2] WebGL2 not supported');
      return;
    }

    const program = createShaderProgram(gl, vertSrc, fragSrc);
    if (!program) return;

    const quad = createFullscreenQuad(gl);
    if (!quad) return;

    const uTime = gl.getUniformLocation(program, 'uTime');
    const uResolution = gl.getUniformLocation(program, 'uResolution');

    const DOWNSCALE_FACTOR = 3;
    const dpr = window.devicePixelRatio / DOWNSCALE_FACTOR;
    let startTime = performance.now();
    let animId: number;

    function handleResize() {
      resizeCanvasToDisplaySize(canvas, dpr);
      gl!.viewport(0, 0, canvas.width, canvas.height);
    }

    window.addEventListener('resize', handleResize);
    handleResize();

    function render() {
      gl!.useProgram(program);
      gl!.uniform1f(uTime, (performance.now() - startTime) / 1000.0);
      gl!.uniform2f(uResolution, canvas.width, canvas.height);

      gl!.bindVertexArray(quad);
      gl!.drawArrays(gl!.TRIANGLES, 0, 6);

      animId = requestAnimationFrame(render);
    }

    render();
  }

  initBackground2();
</script>
